1. Write a Java program that:
	a. define an int array with 5 elements.
	b. use a "for" loop to read an int and put it into the array element in each loop.
	   Use the array.length as the "loop" upper bound number, like:
	   for(int i = 0; i < arr.length; i++). Prompt the user in each loop with a message to enter an integer.
	c. use a "foreach" loop to print out the square of each array element, like:
	   for(int e : arr) {
            System.out.println(e * e);
       }